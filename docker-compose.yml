services:
  app:
    build: .
    container_name: typescript-grid-simulation-engine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev"